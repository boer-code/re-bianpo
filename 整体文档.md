# IoT Raw 设备及站点管理改动说明文档

## 1. 概述
本项目近期对 IoT 模块进行了深度改造，主要围绕 **Raw（原始负载）设备自动化接入**、**数据动态映射配置** 以及 **设备分组向“站点”模式的语义升级** 展开。

---

## 2. 后端改动说明 (IoT 模块)

### 2.1 Raw 设备接入与自动化
- **动态注册支持**：支持基于设备报文中的标识（如 AN/DN）进行设备自动注册入库，无需预先手动创建设备。
- **Raw 产品定义**：引入 `yudao.iot.raw-product-key` 配置项，用于识别哪些设备属于 Raw 协议族。
- **心跳脱离业务流**：独立处理心跳报文，不再进入 Raw 数据映射流程，减少业务日志冗余。

### 2.2 载荷映射 (Payload Mapping)
- **持久化存储**：新增 `iot_device_payload_mapping` 表，用于存储设备报文（Hex 或 JSON）与物模型属性的映射规则。
- **解析增强**：支持从报文解析中动态获取 `cl` 位，支持 JSON 报文数字前导 0，增加报文解析时间戳记录。
- **租户隔离修复**：修复了多租户模式下 Raw 设备解析规则查询失效的问题。

### 2.3 站点管理升级 (原设备分组)
- **语义升级**：将原“设备分组”全面升级为“站点（Site）”，字段保持向后兼容。
- **字段扩展**：新增地区编码、详细地址、站点中心经纬度、海拔高度等地理信息字段。
- **3D 实景支持**：增加地形开关和夸张倍数配置。

### 2.4 设备上下线记录入库
- **新增记录表**：增加 `iot_device_online_record` 表，用于保存设备在线、离线事件历史。
- **统一入口落库**：上下线记录统一在设备状态更新服务中落库，不区分来源，避免各协议/任务各写一套逻辑。
- **覆盖的触发来源**：
  - 离线巡检任务触发的超时离线
  - 设备收到普通上行消息后触发的“强制补上线”
- **幂等控制**：仅在设备状态真实发生变化时写入记录，重复收到相同状态不会重复插入脏数据。
- **网关联动下线**：网关设备下线时，联动下线的子设备同样会各自写入离线记录。

### 2.5 数据大屏匿名接口
- **左上「设备总览」**：接入 `GET /iot/statistics/get-summary`，返回设备总数、在线数、离线数、告警次数等汇总数据。
- **左中「设备数量统计」**：接入 `GET /iot/device-group/simple-list`，返回启用站点列表、站点坐标与各站点设备数量，用于站点设备数量饼图。
- **底部「近30天事件时间线」**：前端复用 `GET /iot/statistics/device-state-records` 与 `GET /iot/statistics/alert-messages` 两个接口，统一传 `startTime`、`endTime` 与 `limitNum`；当前大屏展示口径为 `10` 条告警明细 + `5` 条上下线明细，合并后按时间顺序平铺在地图浮层时间轴上。
- **设备状态事件接口**：`GET /iot/statistics/device-state-records` 支持 `startTime` / `endTime` 时间范围筛选，返回上下线明细列表，并额外返回 `totalOnline`、`totalOffline` 两个 30 天范围统计总数，供时间轴右上角统计展示。
- **右上「消息统计」**：接入 `GET /iot/statistics/get-device-message-summary-by-date`，固定传 `interval=1` 和最近 7 天时间范围；三端统一由 API 层把 `times` 序列化为 `times[0]` / `times[1]`，时间格式为 `YYYY-MM-DD HH:mm:ss`，展示上行消息量与下行消息量趋势。
- **右中「站点告警排名」**：接入 `GET /iot/statistics/alert-ranking-by-device-group?limitNum=8`，按设备所属站点聚合告警次数并返回 Top8。
- **告警事件接口**：`GET /iot/statistics/alert-messages` 同样支持 `startTime` / `endTime` 时间范围筛选，返回告警明细列表，并额外返回 `total` 作为 30 天范围内的告警统计总数。
- **免登录访问**：接口使用 `@PermitAll`，供 `bigscreen/` 无登录态访问；仍需透传租户请求头以命中对应租户数据。
- **状态记录聚合字段**：设备状态接口返回设备名称、在线/离线状态、记录时间、设备坐标与所属站点信息，减少大屏二次请求。

---

## 3. 前端改动说明 (rebianpo-ui / web-antd)

### 3.1 设备管理
- **映射配置标签页**：设备详情页针对 Raw 产品新增“映射配置”Tab。
- **组件复用**：映射配置逻辑封装为独立组件，在“设备发现”和“设备详情”页共用。

### 3.2 站点与 3D 可视化
- **站点管理 UI**：替换“分组”文案为“站点”，增加坐标拾取、地区选择等功能。
- **3D 视角集成**：接入 Vue for Cesium 与天地图地形服务，支持 3D 实景展示。

---

## 4. 关键配置与环境要求 (交接必备)

### 4.1 后端配置项清单
作为接手者，你需要确保以下配置在 `application.yaml` 或 Apollo/Nacos 中正确设置：

#### A. IoT 主服务 (Biz)
- `yudao.iot.raw-product-key`: (必须) 指定 Raw 产品的 ProductKey（通常为 `raw_product`），必须与数据库 `iot_product` 表中对应的记录一致。

#### B. IoT 网关 (Gateway)
- `yudao.iot.gateway.protocols.mqtt.raw-topic-up`: (必须) 原始报文上报的 MQTT Topic（如 `/iot/boer/up`）。
- `yudao.iot.gateway.protocols.mqtt.raw-register-product-key`: (必须) 自动注册时关联的产品标识。
- `yudao.iot.gateway.protocols.mqtt.rawUsername / rawPassword`: 内部认证使用的账号密码。

### 4.2 数据库要求
1. **种子数据**：`iot_product` 表中必须预先存在一条 `product_key = 'raw_product'` 的记录，否则自动注册的设备无法归属产品。
2. **时序数据库**：TDengine 必须处于运行状态，且 `yudao-server` 的 `tdengine` 数据源配置正确，否则无法存储设备上报的历史载荷。

### 4.3 前端环境变量
- **天地图 Token**: 3D 山体展示依赖天地图 API，需在 `.env` 或配置中提供有效的 `VITE_TIANDITU_TOKEN`（或在 `SiteTerrainMap.vue` 中确认）。
- **Cesium 资源**: 确认静态资源路径配置正确，支持 3D 加载。

---

## 5. 调试与排障建议

### 5.1 如何验证 Raw 设备接入？
1. **MQTT 监听**：订阅 `raw-topic-up` 指定的主题，观察是否有原始报文上报。
2. **数据库检查**：查看 `iot_device` 是否生成了新的设备记录，且 `product_key` 为 `raw_product`。
3. **日志观察**：关注 `yudao-module-iot-gateway` 日志中的 “开始处理 raw 上报” 字样，以及 `yudao-server` 中的映射解析日志。

### 5.2 映射配置不生效？
- 检查 `iot_device_payload_mapping` 表中是否存在该 `device_id` 对应的配置。
- 确认物模型标识（Identifier）与映射表中的 `thing_model_identifier` 是否严格匹配（大小写敏感）。

---

## 6. 待办与后续计划
1. **动态算式引擎**：完善数值归零、系数转换功能的后端算式执行逻辑。
2. **站点大屏联动**：进一步优化位置接口在大屏高频调用下的性能。
3. **Raw 映射查询缓存**：对 Raw 上报链路中最重的映射查询处理增加 Redis 缓存，优先缓存 `deviceId -> enabled payload mappings`，减少重复查库与频繁对象转换的开销。
4. **设备重复重连排查**：继续跟进旧设备 MQTT 反复重连问题，重点确认客户端是否重复发起 `CONNECT`、实际生效的 `keepAlive` 配置，以及是否正确维持 MQTT 协议层心跳。
